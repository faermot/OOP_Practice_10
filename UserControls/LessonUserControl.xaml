<UserControl x:Class="OOP_Practice_10.UserControls.LessonUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             mc:Ignorable="d"
             x:Name="lessonUC">

    <Border Padding="0 10" CornerRadius="5">
        <Border.Style>
            <Style TargetType="Border">
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#EFEFEF"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </Border.Style>

        <StackPanel Orientation="Horizontal">
            <Border Background="{Binding Path=Color, ElementName=lessonUC}"
                    CornerRadius="50"
                    Width="40" Height="40"
                    Margin="8 0 0 0"
                    Padding="3">
                <fa:ImageAwesome Icon="Play"
                                 Width="12" Height="12"
                                 Margin="3 0 0 0"
                                 Foreground="#FFFFFF"/>
            </Border>

            <StackPanel Margin="10 0 0 0">
                <TextBlock Text="{Binding Path=Title, ElementName=lessonUC}"
                           Foreground="#303A4F"
                           FontWeight="SemiBold"
                           FontSize="13"/>
                <TextBlock Text="{Binding Path=Description, ElementName=lessonUC}"
                           Foreground="#ACB0B8"
                           FontSize="11"
                           Margin="0 3 0 0"/>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
